{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "System user with role and department assignment",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["Admin", "Department Head", "Volunteer", "Staff Operative", "Finance Head", "HR Head", "Project Head", "Marketing Head", "new_volunteer"] },
        "department": { "type": "string" },
        "isActive": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" },
        "fcmToken": { "type": "string" },
        "onesignalId": { "type": "string" },
        "lastSignalUpdate": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "name", "role", "department", "isActive", "createdAt"]
    },
    "Meeting": {
      "title": "Mission Pulse",
      "description": "Scheduled meeting or mission event",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "start": { "type": "object" },
        "end": { "type": "object" },
        "type": { "type": "string", "enum": ["global", "mission", "departmental"] },
        "dept": { "type": "string" },
        "creatorId": { "type": "string" },
        "creatorName": { "type": "string" },
        "attendees": { "type": "object" }
      },
      "required": ["title", "start", "end", "type", "creatorId"]
    },
    "Attendance": {
      "title": "Mission Attendance",
      "description": "Volunteer punch-in/out record with geolocation",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "projectId": { "type": "string" },
        "missionName": { "type": "string" },
        "location": { 
          "type": "object",
          "properties": {
            "lat": { "type": "number" },
            "lng": { "type": "number" }
          }
        },
        "punchIn": { "type": "object" },
        "punchOut": { "type": "object" },
        "status": { "type": "string", "enum": ["active", "completed"] },
        "durationMinutes": { "type": "number" }
      },
      "required": ["userId", "projectId", "punchIn", "status"]
    },
    "Innovation": {
      "title": "Volunteer Innovation",
      "description": "Ideas and feedback submitted by volunteers",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "submittedBy": { "type": "string" },
        "submittedByName": { "type": "string" },
        "category": { "type": "string", "enum": ["Education", "Logistics", "Social Media", "Environment", "Community", "Other"] },
        "status": { "type": "string", "enum": ["pending", "reviewed", "archived"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "description", "submittedBy", "category", "status", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/schedule/{eventId}": {
      "schema": "Meeting",
      "description": "Scheduled mission pulses and meetings"
    },
    "/attendance/{attendanceId}": {
      "schema": "Attendance",
      "description": "Volunteer mission punch records"
    },
    "/innovations/{innovationId}": {
      "schema": "Innovation",
      "description": "Ideas and innovations submitted by volunteers"
    }
  }
}
